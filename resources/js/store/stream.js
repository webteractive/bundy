const state = () => ({
  items: []
})

const getters = {
  items: state => state.items
}

const mutations = {

}

const actions = {
  
}

export default {
  state,
  getters,
  actions,
  mutations,
  namespaced: true
}